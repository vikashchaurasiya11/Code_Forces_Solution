#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>
#include <cmath>
#include <map>
using namespace std;
typedef long long ll;

ll solve( vector<ll> &dp(),n){

  if
  for(int i=0;i<2;i++)
    for(int j=0;j<len;j++){
      ll inc= a[0]*10 + solve(dp,n-1);
      ll exc= a[1]*10 + solve();

    }
  }
}



int main(){
  int t;
  cin>>t;
  while(t--){
    ll d,n;
    cin>>d>>n;

    vector<ll>a(n);
    for (ll i=0;i<n;i++) {
      cin>>a[i];
    }

    string str= to_string(d);
    ll len= str.length();
    cout<<len<<endl;

    ll ans=INT_MAX;

    vector<ll>dp();
    for(ll i=0;i<len-1;i++){

    }
   

      

    ll cur=llabs(d- res);
    ans=min(ans,cur);
  



    
  }
  return 0;
}  